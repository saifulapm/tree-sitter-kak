==================
Simple evaluate-commands
==================

evaluate-commands %{echo hello}

---

(program
  (evaluate_commands
    (argument
      (percent_string
        content: (string_content)))))

==================
Evaluate-commands with switches
==================

evaluate-commands -no-hooks -draft %{echo hello}

---

(program
  (evaluate_commands
    (switch)
    (switch)
    (argument
      (percent_string
        content: (string_content)))))

==================
Simple execute-keys
==================

execute-keys giGl

---

(program
  (execute_keys
    (argument
      (word))))

==================
Execute-keys with switches
==================

execute-keys -draft giGl

---

(program
  (execute_keys
    (switch)
    (argument
      (word))))

==================
Evaluate-commands with double-quoted string argument
==================

evaluate-commands "echo hello"

---

(program
  (evaluate_commands
    (argument
      (double_quoted_string
        (string_content)))))

==================
Evaluate-commands with single-quoted string argument
==================

evaluate-commands 'echo hello'

---

(program
  (evaluate_commands
    (argument
      (single_quoted_string
        (string_content)))))
